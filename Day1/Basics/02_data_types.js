let name="Sahid"
let rollNumber=12
let isHuman=false
let hasJob=null
let wouldHaveJob;
//symbols are objects as well are based on uniqueness

console.table([typeof(name),typeof(typeof(name)),typeof(rollNumber),typeof(isHuman),typeof(hasJob),typeof(wouldHaveJob)])
// string, string, number, boolean, TODO:object, undefined